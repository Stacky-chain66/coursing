<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact Enquiries</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    /* Animated Royal Blue + Black Gradient Background */
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #000000, #002147, #003366, #000000);
      background-size: 400% 400%;
      animation: bgAnimation 12s ease infinite;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    @keyframes bgAnimation {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Container with Glass/Glow Effect */
    .container {
      max-width: 1200px;
      width: 90%;
      padding: 25px;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.5);
    }

    h1 {
      color: #1e90ff;
      text-align: center;
      margin-bottom: 20px;
      text-shadow: 0 0 12px rgba(30, 144, 255, 0.8);
    }

    /* Table Styling */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      border-radius: 8px;
      overflow: hidden;
    }

    th, td {
      padding: 12px 15px;
      border: 1px solid rgba(255, 255, 255, 0.15);
      text-align: left;
    }

    th {
      background: linear-gradient(90deg, #000000, #003366);
      color: #fff;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    tr:nth-child(even) {
      background: rgba(255, 255, 255, 0.05);
    }

    tr:nth-child(odd) {
      background: rgba(255, 255, 255, 0.08);
    }

    tr:hover {
      background: rgba(30, 144, 255, 0.25);
      transition: background 0.3s ease;
    }

    td {
      color: #fff;
    }

    /* Back Link Button */
    .back-link {
      display: inline-block;
      margin-top: 20px;
      color: white;
      background: linear-gradient(90deg, #003366, #1e90ff);
      padding: 10px 20px;
      border-radius: 6px;
      text-decoration: none;
      font-weight: 600;
      transition: background-position 0.4s ease, transform 0.3s ease;
      background-size: 200% 200%;
    }

    .back-link:hover {
      background-position: 100% 0;
      transform: scale(1.05);
      text-shadow: 0 0 8px rgba(255, 255, 255, 0.8);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Contact Enquiries</h1>
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Message</th>
        </tr>
      </thead>
      <tbody>
        {{#each enquiries}}
          <tr>
            <td>{{this.name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.message}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
    <div style="text-align:center;">
      <a href="/admin" class="back-link">← Back to Home</a>
    </div>
  </div>
</body>
</html>
